#include <bits/stdc++.h>

//?? Most operations in a multi-map is in O(log n)

//*Multi-Map is a map that can have duplicate keys.

using namespace std;
int main() {
  multimap<int, int> mp1; //! Declares a map with {int : int} pair

  //* Every functions similar as map

  return 0;
}