#include <bits/stdc++.h>

/*
?? Unordered sets have a better time complexity than sets in most
?? cases except when collisions occure

?? Avg Case =  O(1)
?? Worst Case (Very Very Rare) = O(n)
*/

//*It works the same as set except, all the entries are randomized.

using namespace std;
int main() {
  unordered_set<int> st;

  /*
   * All of the functions are same as set except lower_bound() and
   * upper_bound() they don't worn because the set is unordered
   */

  return 0;
}