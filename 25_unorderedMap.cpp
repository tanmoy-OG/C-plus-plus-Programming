#include <bits/stdc++.h>

//?? Most operations in an unordered-map is in O(1) while in worst cases O(n)

//*Unordered-Map is a map that has unique keys but is not sorted.

using namespace std;
int main() {
  multimap<int, int> mp1; //! Declares a map with {int : int} pair

  //* All functions similar as map except lower and upper_bound() is impossible

  return 0;
}